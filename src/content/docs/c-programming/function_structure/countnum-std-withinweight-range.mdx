---
title: "Count Students by Weight Range"
description: "Pass arrays to functions to count students in a weight range."
tags: ["C Programming", "Functions", "Arrays"]
---

import DifficultyBadge from '../../../components/DifficultyBadge.astro';

<DifficultyBadge level="Intermediate" />

This program demonstrates passing a 1-D float array representing weights to a given function, and then having the function iterate over the array elements to return a count.

```c
#include <stdio.h>

int main()
{
    float w[5]; // weight array is float because weight can be a decimal number i.e 55.5 kg
    int i, Co;

    printf("Enter the weights of 5 students in kg: ");
    for(i=0;i<5;i++) 
    scanf("%f",&w[i]);
    Co = check(w); /* The value in m of check function is received by check(w) and 
                    assigned to Co in main function.*/
    printf("\nThe number of students in the weight range 50-60 kg = %d", Co);
}

int check(float w[]) // Here the passed array is catched as a pointer to the first element.
{
    int i,m=0;
    for(i=0;i<5;i++)
        if(w[i]>=50 && w[i]<=60) 
        m = m + 1;
    return m;
}
```
